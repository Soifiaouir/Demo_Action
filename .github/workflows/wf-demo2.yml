name: Workflow Local
on: [push, workflow_dispatch]
jobs:
    buildJob:
        runs-on: ubuntu-latest
        steps:
            - name: Simulation d'exécution du Build
              run: echo "Buil en cours"

    testJob:
        runs-on: ubuntu-latest
        steps:
            - name: Simulation d'éxécution du Test
              run: echo "Test en cours"